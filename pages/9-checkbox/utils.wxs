function sum (dataList) {
    var sum = 0;
    for(var i = 0;i < dataList.length; i++){
        if(dataList[i].isChecked){
            sum += dataList[i].price * dataList[i].num;
        }
    }
    return sum;
}

module.exports = sum;